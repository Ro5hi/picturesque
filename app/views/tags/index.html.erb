<div class="alert alert-info">
    <% if flash[:notice] %>
        <%= flash[:notice] %>
    <% end %>
</div>

<div class="container">
  <ul>
    <% @tags.each do |tag| %>
    <li><%= link_to "#{tag.name}", "tag/#{tag.name}" %></li>
    <% end %>
  </ul>
</div>