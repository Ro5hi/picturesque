<div class="container">
    <div class="row">
        <div class="col-8">
            <% @posts.each do |post| %>
                <%= image_tag post.photo.url, class: "img img-fluid"%>          
            <% end %>
        </div>
    </div>
</div>