<%= link_to 'All posts', posts_path %>
<h1><%= @post.title %></h1>
 
<%= image_tag(@post.photo.url, alt: 'Photo') if @post.photo? %>
 
<p><%= @post.body %></p>
 
<p><%= link_to 'Edit', edit_post_path(@post) %></p>